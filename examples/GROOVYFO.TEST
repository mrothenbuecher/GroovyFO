/*
* Beispiel GroovyFO holt die ersten 100 Teile aus der DB
* und gibt deren Suchwort aus
* ow1/GROOVY.TEST
*/

for(def i=0; i<100 && hole("Teil","@Gruppe=1;"); i++){
        // Ausgabe auf Konsole
        println h.such
        //alternativ auch println h("such") oder println h|such
        
        // Test Dachoperator
        println h.abplatz^such
        
        // Test typcast
        println "Mindestbestand: "+h.mindest+" Mindestbestand+1:"+(h.mindest+1)
}
